2026-09-06 stage-1: Closed the 19 dead ends in goldilocks-assessment §2.4 (10 shredded knowledge lessons rebuilt from their writeback payloads, 2 skills registered, objective.md/--force/loop.max_iterations/contradiction/adapter items fixed) and added three blocking eval.sh checks (referenced-paths, skill-manifest-parity, knowledge-integrity) plus the phase-state.py writeback normalization, so Story 5's baseline measures a corpus whose references resolve rather than a drifted one.
2026-09-06 stage-1: Selected the four yuss baseline stories (api_route 79d79ae, ui 8d97930, data_model c9350fc, refactor 12eea11 at yuss 7c2d043) with a user-approved per-test-file live-service rule (--live-test-scope file, echoed in criteria) because the spec's story-level rule fills only ui and api_route on the real archive; the baseline is a two-spec, one-week sample and says so in criteria.
2026-09-06 stage-1: measure-invocation.py counts tokens with Anthropic count_tokens (--tokenizer auto|anthropic|estimate, claude-fable-5-1, hash-only cache under .writ/state/, per-item degradation, 401 fatal) so Story 5's baseline records measured tokens instead of chars/4; no-key output stays byte-identical except token_note; the first real-key run is pending a maintainer and is the step that recalibrates the ratio ADR-021/023 call unvalidated.
2026-09-06 stage-1: pipeline-baseline.py run/ingest builds an isolated checkout at parent_sha (never a worktree), overlays current Writ, invokes claude headless with bypass permissions, and writes one scrubbed run record per (story, n) so Story 5's eight-run capture and compare share one schema (RUN_KEYS); live smoke run deferred — no ANTHROPIC_API_KEY in the implementing environment.
2026-09-07 stage-1: Captured the Fable 5.1 pipeline baseline (8 runs, 4 yuss stories × 2, all exit=met, $187.60 driver cost / 5.5 h) as .writ/eval/baselines/2026-09-06-claude-fable-5-1.json gated by validate + eval.sh check_pipeline_baseline and diffable with compare, so every Stage 2 cut is judged on per-story per-metric deltas; two runs kept over one because cache_read varied 59% between runs of the same story, and the task 5.6 cost formula understates the driver's accounting 3.6× so cost_usd is the price of record.
2026-09-07 stage-2: Story 1 — ADR-026 (constraint test: environment fact / human boundary stay, behavior request leaves), empty append-only ledger, prune-ledger.py check/measure (git -U0 diff against cf84742, per-file move cancellation, five finding codes, exit 0/1/2), and the eval.sh pruned-base check landed with 0 findings; base measured at 28,157 bytes over the 10,000 cap as a note until Story 3 appends <!-- cap: blocking -->; blank lines exempt from the ledger (DEV-002) because the literal rule was unsatisfiable once a section leaves whole.
2026-09-07 stage-2: Story 4 — gates: frontmatter block, verdict-provenance.py check, eval.sh verdict-provenance check landed: commands/implement-story.md declares all ten Step 3 gates' verdict sources (2 script, 8 prose-only against the Goal Card's cap of 2) and the check reports the gap as a note, so the mechanization spec starts from a checked count rather than an impression; Gate 4.5's match percentages dropped from the command body because no diff produced them.
2026-09-07 stage-2: Story 2 — four documentation sections moved out of system-instructions.md into .writ/docs/ (Model Tiers + entry_level merged into model-tiers.md, required_skills convention + Skill authoring merged into skills.md, Startup Update Awareness and the Recommendation Semantics tutorial to new files), one commit per move carrying its ledger rows (102 rows, class moved), each leaving one pointer line; base measured 28,157 -> 15,713 bytes (system-instructions.md 10,061 + _preamble.md 5,652) so Story 3 cuts about 5,700 bytes to reach the 10,000 cap; two governor pin sets (check_recommendation_semantics, MechanismRecordTests) follow the text to the docs, and cursor/writ.mdc took only the Prime Directive cut (DEV-005..008).
2026-09-07 stage-2: Story 3 — behavior requests cut, base at 9,704 bytes, cap flipped to blocking: every line of both base files classified (environment fact / human boundary / behavior request) in the story's What Was Built before cutting, then 85 lines left with ledger rows in their own commits (67 behavior-request, 18 duplicate; 187 rows total) — Identity & Approach, Command Execution Protocol, Judgment Principles, Prose, the duplicate File Organization, Interaction Tool Selection beyond its rule line, Session Auto-Orientation, the Skills explainer, and the preamble's Tool Selection, Knowledge Context, Adapter Neutrality — with every kept line byte-identical to cf84742; the base's generic parallel-call nudges became one Fable 5.1 line per adapter under ## Model-specific, cursor/writ.mdc was re-synced whole because it is the rule Cursor loads (DEV-011), and the adapters/cursor.md grep proxy reads 3 because of a pre-existing verification record (DEV-009).
2026-09-08 stage-2: Re-ran the Fable 5.1 baseline on the pruned base (9,704 bytes) — 8/8 exit criteria met vs Stage 1's 8/8, driver cost $178.69 vs $187.60 (−4.7%, inside run-to-run noise), cache-creation tokens −36%; the cut is KEPT under Business Rule 10. Two first attempts were killed by the headless driver (600 s background-task ceiling; session teardown), not the model, and were retried once each. Committed as .writ/eval/baselines/2026-09-07-claude-fable-5-1.json.
2026-09-08 stage-2b: Story 1 — review-override.py, Gate 3 verify block, gate3_review script source, eval.sh review-override check landed. FAIL-only: mechanical fail blocks; mechanical pass does not wash out review-agent FAIL/PAUSE.
2026-09-08 stage-2b: Story 2 — arch-check.py re-derives Gate 0 proceed/caution (never abort); --planned is the live mode.
2026-09-08 stage-2b: Story 3 — docs-check.py diffs public exports against README/CHANGELOG/framework docs/docstrings; Writ-the-product is unverifiable.
2026-09-08 stage-2b: Story 4 — boundary-map.py compute + change-surface.py classify land; Gate 0.5/2.5 pass stdout; maps stay advisory.
2026-09-08 stage-2b: Story 5 — drift-format.py; gates flipped to 8 script / 2 prose-only; --prose-only-blocking on; watch field; replay totals {54 agree / 42 note / 48 unverifiable}
2026-09-08 stage-3: Story 1 — spec-analyze.py check: structural codes + findings-JSON schema; unverifiable when --spec missing or --findings omitted with no structural hit.
2026-09-08 stage-3: Story 2 — create-spec Step 2.6c after 2.6b + verify-spec 3g; Goal Card “Step 2.6” relocated after stories exist; analysis notes never fail the package.
2026-09-08 stage-3: spec-analyze.py advisory; hooks after 2.6a + verify-spec; precision {contradiction 1/0, gap 1/0, ambiguity 1/0, clean 1/0; overall 4/0}
2026-09-09 stage-4a: Story 1 — goal-emit.py emit/check: GOAL.md + VERIFY.md + pinned /goal invoke after summary; unverifiable missing_card/loop_no; fail malformed_card/missing_boundary.
2026-09-09 stage-4a: Story 2 — hooks only: create-goal emit after save; implement-phase emit when Origin:/spec_ref resolves; notes for pass/fail/unverifiable; finding for missing helper or exit 2.
2026-09-09 stage-4a: Story 2 repair — implement-phase.md trimmed to 35160 bytes (overage 10200); KNOWN_OVER_BUDGET lowered 10208 → 10200. Step 1.4 emit contract kept.
2026-09-09 stage-4a: naming emit + hooks + gold — goal-emit.py + create-goal/implement-phase hooks + adapter paste-the-emit-line + eval.sh goal-emit + loop-yes/loop-no gold equality. Live /goal not run; gold files are the proof.
2026-09-09 stage-4b: Story 1 — evaluator-agent.md + Claude/Codex peers, parity mapping, manifest + SKILL.md catalog; review-agent and spawn unchanged.
2026-09-09 stage-4b: Story 2 — implement-story.md default is coding-agent + evaluator-agent; --full-pipeline is the six-agent hatch; two-fail escalate; Gate 4 default fail recodes via coding-agent.
2026-09-09 stage-4b: evaluator + default spawn + spawn-cap.py / eval spawn-cap; adapters route full SDLC to --full-pipeline.
2026-09-25 flagged-harness-cuts: Story 5 keep-or-revert — NULL. Model claude-opus-5-5; sample 1 story × 2 per arm (reduced, user-approved DEV-010; keep requires 4 × 2); story 2026-07-23-quick-split-single-transaction/story-3-settlement-view-share-link; Writ 78d422c clean. Exit criteria 2/2 control, 2/2 lean. cost_usd control $12.56 ($6.85, $5.71), lean $10.75 ($5.03, $5.72), delta -$1.81 — per-run costs overlap; lean used more turns (89 vs 84) and cache reads (7.02M vs 6.37M); formula_usd $4.49 vs $4.67 (does not decide). Default load path unchanged; install/update do not ship *.lean.md. Evidence: .writ/eval/baselines/2026-09-25-lean-arms/, scripts/lean-decision.py decide.
2026-09-25 jev-pilot: ADR-027 and jev-judge.py status resolve the double opt-in — provider enabled only when .writ/config.md names typesafe or vercel-gateway AND that backend key is non-empty; a key alone never sends data; the key value is never printed. Verifier scripts stay key-free; Jev replaces a verdict source, not an agent tier (ADR-024 D3 unchanged); shadow rows are not ADR-025 signals.
2026-09-25 jev-pilot: Story 2 — jev-judge.py gains judge() (stdlib urllib POST /v1/systemone, 10s timeout, 429/529 retry ×3 with retry-after or 1/2 s backoff, state-size guard at 30,000 est. tokens, every outcome mapped to a named reason), a replay transport keyed by SHA-256 of the canonical body that always wins over live, and a `probe` subcommand; jev-thresholds.json ships the conservative §4 values uncalibrated; eval.sh `jev-judge` check runs status + replayed probe with keys stripped, so eval never touches the network or a key.
2026-09-25 jev-pilot: Story 3 — jev-judge.py spec-findings judges every story of a spec in one request (per story: Noul contradiction and gap; per criterion: Noul ambiguity; state keyed by story filename, paths referenced by backtick), applies emit/escalate from jev-thresholds.json in code, and writes a spec-analyze.py --findings array (extra keys source, p; summaries code-generated) plus <out>.escalate.json; any unverifiable or fail writes [] and escalates every story, so the orchestrator runs its full pass. /create-spec Step 2.6c runs it first when status prints pass and limits the orchestrator pass to escalated stories; /verify-spec 3g carries the same conditional; every outcome stays a note. eval jev-judge adds a replayed spec-findings run on synthetic fixtures.
2026-09-25 jev-pilot: Story 5 — jev-judge.py ac-shadow asks one Noul per acceptance criterion (state keyed by AC ID, not list position; one request per story) against the recorded test output and a diff slice that drops whole file blocks naming .env*/*.pem/*.key/*secret*/*credential* paths (counted as excluded_paths; splits on diff --git/--cc/--combined and header-less ---/+++ pairs outside counted hunks, and fails closed with unverifiable secret_path_unparsed, sending nothing, if any header-like line left in the kept text still names a secret path), parses the evaluator verdict from `- [x]`/`- [ ]` checklist lines ending in an [AC-N.M] tag, and appends one JSONL row (p, jev = p >= ac_shadow.satisfied, evaluator, agree; no server text) to .writ/state/jev-shadow.jsonl only when the judgment passes; no tags gives unverifiable no_evaluator_ids with no request and no row. shadow-report reads only the log and returns pass promotion_met only at >=30 distinct stories, 0 false passes, and >=95% per-criterion agreement. evaluator-agent.md checklist lines gain the tag; /implement-story Gate 3 runs ac-shadow after review-override when status passes, as a note that never changes PASS/FAIL/PAUSE, the review loop, or DEGRADED. Evidence for a later conditional-spawn spec only; nothing reads it to skip a spawn.
2026-09-25 jev-pilot: calibrated thresholds from 24 fixtures — 24 labeled spec-analyze fixtures (4 legacy + 20 synthetic-*, 6 per class; labels model-authored), split 12 dev / 12 test in splits.json. Wording (DEV-019) was tuned on dev only, with every example word generic or taken from dev fixture text: contradiction keeps its Story 3 text; gap asks one literal condition (an input the user supplies with a valid-case criterion and no absent/empty/unknown/invalid-case criterion; examples keyword, file, link, token, form field; tests, builds, and verdicts are not inputs) instead of listing six failure kinds; ambiguity asks whether the Then fails to decide its outcome (an undefined judgment word, or a side effect where the Given needs a decision, e.g. whether a fee is assessed); criteria are keyed by AC ID (C<n> when untagged), not list position. A first calibration was withdrawn at Gate 3 because its gap examples card, id, and import row came only from test-split fixtures; its 24 recordings were deleted and re-made: one dev-only calibrate --live trial (12 requests) became the dev recordings, then the 12 test fixtures were recorded once and scored unchanged (84 live vercel-gateway requests in all, fixture content only). Disclosure: after the first dev wording trial, dev clean fixture synthetic-clean-api-rate-limit AC-4.1 was edited from "each request is served normally" (an earlier ambiguity variant scored it 0.81; the final ambiguity wording was never asked on the old text) to "no request receives a `429` response"; all recordings postdate the edit. calibrate fits per class on dev: emit = lowest gold score at least 0.05 above the highest clean score, escalate = min(clean max + 0.01, lowest gold score): contradiction 0.22/0.10, gap 0.72/0.29, ambiguity 0.39/0.10 (calibrated: true, vercel-gateway, typesafe-ai/jev, calibrated_on 2026-09-25). Test split: 0 clean false positives, 0 silent misses; contradiction 2/3 and ambiguity 2/3 emitted (the misses escalate); gap 3/3 with 2 false positives on non-clean stories; 11 of 12 test stories escalate, so the cascade is safe but saves little orchestrator work. install.sh, update.sh, and unlink.sh ship scripts/jev-thresholds.json (DEV-006).
