# Story 5: Entry-Level Check — Every Command Declares What It Expects You Chose

> **Status:** Not Started
> **Priority:** Medium
> **Dependencies:** Story 1

## User Story

**As a** Writ maintainer implementing ADR-024 (command author)
**I want to** confirm the `entry_level` derivation table with one bounded question, declare `entry_level: high | standard | any` in the existing `---` frontmatter of all 31 `commands/*.md`, verify the one-line entry-notice rule lives exactly once in `system-instructions.md` § Model Tiers (mirrored in `cursor/writ.mdc`) and nowhere in any command, and give the field two consumers — a `lint-skill.sh` value check with a script test and a non-blocking `eval.sh` presence note
**So that** the field ADR-016 left inert gets a live reader: a user who enters a command below the weight it expects sees one advisory line and continues, every command's expectation is derivable from two questions rather than taste, and a future command missing the field is noticed without breaking the eval gate

## Acceptance Criteria

> **AC IDs assigned through:** AC-5.5

- [ ] Given the maintainer has answered one AskQuestion confirming (or re-deriving by Q1/Q2) the spec.md "`entry_level` derivation applied" table, when `rg -c "^entry_level:" commands/*.md` runs after the edits, then exactly 31 files match with one hit each, every value equals the confirmed table's level for that command (14 `high`, 12 `standard`, 5 `any` unless a re-classification was justified by Q1/Q2 in the AskQuestion), the line sits after `outcome:` and before `exit_criteria:` in each file, and `commands/_preamble.md` carries no `entry_level:`. `[AC-5.1]`
- [ ] Given `system-instructions.md` § Model Tiers after Story 1, when the technical-spec §5 notice text (`This command expects \`<level>\` entry; you're running \`<model>/<effort>\`. Floor-tier retries cannot escalate above this. Consider re-running at a higher thinking level.`) is searched for across the repository, then it appears exactly once in `system-instructions.md` and once byte-identically in `cursor/writ.mdc`, `rg -l "This command expects" commands/` returns nothing, and if Story 1 omitted the text this story has added it to § Model Tiers and mirrored it before any command edit. `[AC-5.2]`
- [ ] Given a command entered at an origin the running model self-assesses as below its `entry_level`, when the command starts, then the contract text directs it to print the §5 notice as exactly one line at most once per session and continue; it never issues an AskQuestion, never blocks, and when `anchor.model` or `anchor.effort` is `unknown` the check is skipped with nothing printed — verified by reading the contract text and by one manual run of an `any`-level command (no line) and one `high`-level command at a deliberately low origin (one line, command proceeds). `[AC-5.3]`
- [ ] Given a fixture command file carrying `entry_level: max` (and a second carrying `entry_level: High`), when `bash scripts/lint-skill.sh <fixture>` runs, then it prints a `❌` finding naming `high`, `standard`, `any` and exits 1; and given all 31 real `commands/*.md`, when the lint runs over them, then it exits 0 with no `entry_level` finding. `[AC-5.4]`
- [ ] Given a synthetic project tree in which one `commands/*.md` other than `_preamble.md` lacks `entry_level:`, when `bash scripts/eval.sh` runs from a copy of the script in that tree, then the report carries one `add_note` line naming the file and the eval still ends `Findings: 0` with exit 0; and given the real repository after this story, then the note does not fire and `bash scripts/eval.sh` ends `Findings: 0`. `[AC-5.5]`

## Implementation Tasks

- [ ] 5.1 Write the tests first. Add `scripts/tests/test_lint_entry_level.sh` following the `fail`/`ok`/`mktemp -d` convention of `scripts/tests/test_eval_length_caps.sh`: fixtures for `entry_level: max` and `entry_level: High` (expect `❌`, exit 1, output names `high`, `standard`, `any`), and a run over `commands/*.md` asserting exit 0 and zero `entry_level` findings — this last case must fail on first run because no command carries the field yet. Add `scripts/tests/test_eval_entry_level_note.sh` using the copy-`eval.sh`-into-a-temp-`scripts/`-dir harness from `test_eval_length_caps.sh`: a synthetic `commands/` with two frontmatter-bearing files, one lacking `entry_level:`, asserting one note naming that file, `Findings: 0`, exit 0; and a `_preamble.md` lacking the field producing no note. Confirm the eval test fails before 5.6. `[AC-5.4, AC-5.5]`
- [ ] 5.2 Confirm the table before any command edit. Present one AskQuestion to the maintainer showing the spec.md "`entry_level` derivation applied" rows (14 `high` / 12 `standard` / 5 `any`) with options "Confirm as written (Recommended)" and "Re-classify specific commands"; for any re-classification, record in the story's Notes the command, the new level, and which of Q1 (spawns agents / locks a contract / renders an unverified judgment) or Q2 (creates or modifies durable artifacts) drives it — a change with no Q1/Q2 justification is declined, not applied. Update the spec.md table if it changes. `[AC-5.1]`
- [ ] 5.3 Verify Story 1's contract text. Run `rg -n "This command expects" system-instructions.md cursor/writ.mdc commands/` and check § Model Tiers states the §5 blockquote verbatim: one line, at most once per session, never a question, skipped when origin is `unknown`, with the `high`/`standard`/`any` self-assessment guidance. If present once in each root file and absent from `commands/`, record that and move on. If Story 1 omitted it, add the blockquote to `system-instructions.md` § Model Tiers and copy the section byte-identically into `cursor/writ.mdc` (leave the trailing `## Self-Dogfooding` appendix untouched); never add it to `commands/_preamble.md` (at its 95-line cap) or to any command. `[AC-5.2, AC-5.3]`
- [ ] 5.4 Declare the field in all 31 commands. For each of `assess-spec create-adr create-issue create-spec create-uat-plan design edit-spec implement-phase implement-spec implement-story initialize knowledge migrate new-command new-skill plan-product prototype refactor refresh-command reinstall-writ release research retro review revert security-audit ship status uninstall-writ update-writ verify-spec`, insert `entry_level: <value>` from the confirmed table as a new line directly after the `outcome:` line and before `exit_criteria:` inside the `---` frontmatter — an unquoted bare scalar at column 0, no trailing whitespace, never inside the `exit_criteria:` list. Do not touch `commands/_preamble.md`. After the sweep, run `bash scripts/eval.sh` (its `check_exit_criteria` pins read command frontmatter) to prove the frontmatter still parses. `[AC-5.1]`
- [ ] 5.5 Wire the lint consumer. Story 1 shipped the `^entry_level:` grammar branch in `scripts/lint-skill.sh`; run `bash scripts/tests/test_lint_entry_level.sh` from 5.1 until it passes, then `bash scripts/lint-skill.sh commands/*.md` and confirm zero `entry_level` findings across all 31. If the grammar branch is missing or does not anchor on `^entry_level:`, fix it in `lint-skill.sh` to match technical-spec §1 rather than working around it in the test. `[AC-5.4]`
- [ ] 5.6 Wire the eval consumer. In `scripts/eval.sh`, add an `entry_level` presence pass (inside the existing command-structure check or as a small helper in the same style): for every `commands/*.md` except `_preamble.md`, if `grep -Eq '^entry_level:' "$file"` fails, call `add_note "NOTE [$rel]: no entry_level: in frontmatter (ADR-024 A3). Derive it by Q1/Q2 — see system-instructions.md § Model Tiers."` — `add_note`, never `add_finding`, following the ~line 444 comment's precedent. Run `bash scripts/tests/test_eval_entry_level_note.sh` until it passes. `[AC-5.5]`
- [ ] 5.7 Verify end to end: `rg -c "^entry_level:" commands/*.md` → 31 files, each count 1; `rg "^entry_level:" commands/*.md | sort` matches the confirmed table level-for-level (14/12/5 or the recorded re-classification); `rg -c "^entry_level:" commands/_preamble.md` → 0; `rg -l "This command expects" commands/` → empty; `diff <(sed '/^## Self-Dogfooding/,$d' cursor/writ.mdc) system-instructions.md` → empty; `bash scripts/lint-skill.sh commands/*.md skills/*/SKILL.md` clean; `bash scripts/tests/test_lint_entry_level.sh` and `bash scripts/tests/test_eval_entry_level_note.sh` pass; `bash scripts/eval.sh` (run outside the sandbox — it does `git init` in a temp dir) ends `Findings: 0` with no `entry_level` note; one manual run each of `/status` (no notice line) and `/create-spec` at a deliberately low origin (exactly one notice line, command continues, no AskQuestion). `[AC-5.1, AC-5.2, AC-5.3, AC-5.4, AC-5.5]`

## Notes

**Self-assessment can be wrong both ways.** The entry check is the running model judging its own origin against plain guidance (`high`: frontier-class model of its family at a non-minimal thinking level; `standard`: non-smallest model or medium-plus effort; `any`: nothing). Writ maintains no model ranking (Business Rule 9), so a false notice and a missed notice are both possible. The design accepts this because the cost of either error is one advisory line: it never asks, never blocks, and the command continues (spec.md Technical Concerns, State catalog "Origin below `entry_level`"). Do not add a ranking, a threshold table, or a confirmation prompt to "fix" a wrong self-assessment — that is out of scope (a blocking entry gate is excluded) and would reintroduce the judgment ADR-023 forbids inventing evidence for.

**The check text lives in the root contract, not per command.** Technical-spec §5 is placed once in `system-instructions.md` § Model Tiers (paid once per invocation) and mirrored into `cursor/writ.mdc`. Commands carry only the declaration. Repeating the blockquote in 31 files would multiply the byte budget hit and create 31 drift points; `commands/_preamble.md` is at its 95-line hard cap (`eval.sh` ~line 421 enforces) and absorbs nothing. Story 1 owns the text; this story verifies it and repairs only if it is missing, which is why 5.3 comes before 5.4.

**`_preamble.md` is excluded everywhere.** It is not a command: no `entry_level:`, no lint expectation, and the eval note must skip it explicitly or the note fires on every run.

**Derivation, not taste.** The 14/12/5 table is *proposed* in spec.md and the maintainer confirms it in 5.2 via one bounded AskQuestion — this is a known option space, so Plan Mode is not used. Any re-classification must cite Q1 or Q2; `/implement-spec` and `/implement-story` both stay `high` with no finer ordering (ADR-024 A3). If the maintainer re-classifies, update the spec.md table so the Success Criterion `rg "entry_level:" commands/*.md` → 31 still has a table to be checked against.

**Risks.** (1) Frontmatter edits across 31 files — one malformed YAML line (a stray quote, a tab, a misplaced `- ` list marker, or inserting inside the `exit_criteria:` list) breaks `scripts/exit-criteria.py`'s parse and the `require_literal` pins in `eval.sh` `check_exit_criteria` that read command frontmatter. Insert as a bare scalar on its own line between `outcome:` and `exit_criteria:`, then run the eval before committing. (2) `create-spec.md` and `implement-story.md` are already over the non-blocking byte budget; one 20-byte line each is acceptable per ADR-023's triage and adds no finding. (3) `rg -c` counts lines; a command that mentions `entry_level:` in body prose at column 0 would inflate the count — the `^entry_level:` anchor plus manual inspection of the 31 lines in 5.7 catches it. (4) A test that runs `eval.sh` against the real repo cannot fixture a missing field, hence the temp-root harness borrowed from `test_eval_length_caps.sh`.

**Integration.** Story 1 supplies the `entry_level` lint grammar (`^(high|standard|any)$`, technical-spec §1) and the §5 notice text in § Model Tiers; this story adds the script test, the 31 declarations, and the eval note, and repairs the notice text only if Story 1 left it out. Story 2's scaffolder changes (`commands/new-command.md` emits `entry_level` for future commands) cover new commands; this story covers the 31 that exist. Story 4's `require_literal` pins are for escalation sentences in `create-spec.md`/`implement-story.md`, not for `entry_level` — do not add pins for the notice text to `eval.sh` here; the presence note is the only eval surface this story owns. ADR-025 is untouched.

## Definition of Done

- [ ] All tasks completed
- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Code reviewed
- [ ] Documentation updated

## Context for Agents

- **Error map rows:** [Entry check, Lint (alias value), Lint (unknown value), Read origin]
- **Shadow paths:** [Nil origin]
- **Business rules:** [3 — origin is read never asked; surfaced to the user only by the entry-level notice, 8 — commands carry `entry_level` derived by Q1 (spawns/locks/unverified judgment → `high`) and Q2 (durable artifacts → `standard`) else `any`, 9 — the entry check is one line once per session never a question; model self-assesses against plain guidance; no model ranking]
- **Experience:** [Entry point (command author sees `entry_level: high | standard | any` in the frontmatter they already edit), Feedback model (the one-line notice text — "This command expects `high` entry; you're running `<model>/<effort>`…" — and the command continues), Error experience (nothing hard-fails), State catalog (Origin below `entry_level`, Origin unknown — entry check skipped)]

Reference: `.writ/docs/context-hint-format.md`.
