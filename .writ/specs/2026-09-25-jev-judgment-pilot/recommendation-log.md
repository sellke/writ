# Recommendation Log: Jev Judgment Pilot

> Mode: `/create-spec --recommend --from-issue .writ/issues/improvements/2026-09-25-jev-typed-judgments-for-harness-gates.md`
> Authorized by: user instruction on 2026-09-25 ("if it makes sense, just go ahead and /create-spec")
> ADR: [ADR-013](../../decision-records/adr-013-recommended-autonomous-delivery.md)

| # | Decision | Evidence | Material alternatives | Risk / reversibility | Result |
|---|---|---|---|---|---|
| 1 | Author a spec at all, rather than leave the issue unpromoted | The issue lists 15 candidates. Three have existing seams: `spec-analyze.py --findings`, the `review-override.py` slot after Gate 3, and labeled gold fixtures under `scripts/tests/fixtures/spec-analyze/`. | Stop at the issue; write the ADR only | Low. The spec is unimplemented and can be closed or abandoned | Spec authored |
| 2 | Scope the pilot to two sites (Step 2.6c spec analysis, Gate 3 shadow), not the whole candidate list | Step 2.6c findings are advisory (`create-spec.md` Step 2.6c, item 3), so errors are cheap. The evaluator is the largest recurring spawn on the default path (Stage 4b left coding-agent + evaluator). Covering all 15 sites would be 3–4 separate features | All sites in one spec; evaluator replacement first | Medium if widened. Keeping the other sites out makes each follow-up its own decision | Two sites; the rest listed as follow-ups in the issue |
| 3 | Evaluator gets shadow mode only; no spawn is skipped | A wrong PASS ships an unverified story. The review-override check is FAIL-only (`implement-story.md` Gate 3), so it would not catch a false pass. Jev jaggedness doc: literal reading, indirection | Conditional spawn behind a threshold now | Shadow is fully reversible. Skipping spawns is not, because a false pass leaves no trace | Shadow, plus a promotion rule in ADR-027 |
| 4 | Stdlib `urllib` client, not `typesafe_sdk` | `pyproject.toml` floor is Python 3.9; Writ has no product dependencies (`.writ/docs/quality-signal-classification.md:258`); `python3 -c "import typesafe_sdk"` fails here | Add the SDK as an optional dependency | Low. The HTTP contract is small (`POST /v1/systemone`) | Stdlib |
| 5 | Double opt-in (config line + key) | Spec text and diffs leave the machine. A key in the shell is not project-level consent | Key alone; CLI flag per run | Low. Config-line removal disables | Double opt-in |
| 6 | Put the call in a new `jev-judge.py` caller, not inside `spec-analyze.py` | Phase 11 Stage 3 rule: "Do not add an API key to `scripts/spec-analyze.py`" (`create-spec.md` Step 2.6c) | Extend `spec-analyze.py` | None. The existing schema already accepts extra keys | New caller |
| 7 | Pin `jev-1.13.0`, not `jev-latest` | TypeSafe Models page: aliases move on release; pin a version when thresholds are tuned against it | Alias | Low. Bump deliberately with recalibration | Pinned |
| 8 | Sub-spec set: `technical-spec.md` only (with error map) | External network integration → error mapping applies (Step 2.8 heuristic). No database, no HTTP API served, no UI | Add `api-spec.md` | None | technical-spec only |
| 9 | Visual references: `none` | No user-facing UI; outputs are CLI lines and command notes | `generate` | None | Empty `mockups/` |
| 10 | Dependencies: Stage 3 (spec-analyze seam) and Stage 4b (evaluator default path) | Both specs are complete-family per `scripts/spec-status.py is-complete` | No dependencies | None | Declared |
| 11 | Cross-spec overlap: none | Every `.writ/specs/*/spec.md` resolves complete-family, so there is nothing in progress to compare against | — | None | No overlap section |
| 12 | Owner `@unknown` | `git config user.name` is unset in this repo | — | Cosmetic; edit the header | `@unknown`; the user was warned |
| 13 | Add a `vercel-gateway` backend beside `typesafe` | User has Jev through the Vercel AI Gateway (2026-09-25). Gateway serves TypeSafe's request and response shapes at `https://ai-gateway.vercel.sh/typesafe/v1/systemone`. Live dummy call: 200, Noul 0.01, 280 input tokens | Gateway-only; TypeSafe-only | Low. Same transport code, different base URL, key variable, and model ID | Both backends |
| 14 | Gateway requests always send `providerOptions.gateway.only: ["typesafe-ai"]` | Unrestricted, the gateway tried DigitalOcean first (503), then TypeSafe. With `only`, it tried `typesafe-ai` only (verified live) | Leave routing unrestricted; request `zeroDataRetention` (the model lists `zdr: none`, so that request would likely fail) | Low. Limits who receives spec text | Restricted |
| 15 | Accept alias-only model on the gateway and record `model_unpinned` | `typesafe-ai/jev-1.13.0` → 404 `model_not_found`; responses echo the alias (vercel/ai #21213) | Refuse the gateway until it pins versions | Medium. Thresholds can drift silently when the alias moves; ADR-027 states this | Alias with a recorded calibration date |
| 16 | Add Story 6: a one-time setup prompt at Step 2.6c that writes the config line and never collects a key | User asked that Writ users be prompted for a Jev key or equivalent. A key typed into chat stays in the transcript (it happened in this session) | Prompt in `install.sh`/`/initialize`; ask for the key value | Low. "Never" writes `none` and suppresses the prompt | Story 6, after Story 3 (same file) |
