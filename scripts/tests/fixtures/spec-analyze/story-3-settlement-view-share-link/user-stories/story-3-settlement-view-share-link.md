# Story 3 (gap stand-in)

## Acceptance Criteria

- [ ] Given a settlement, when a user opens the view, then `settlement.html` renders
- [ ] Given a share link, when it is opened, then the view loads
- [ ] Given an expired token, when the link is opened, then the page shows `expired`
