# Story 2 (contradiction stand-in)

## Acceptance Criteria

- [ ] Given an event total of $100, when three equal splits are saved, then each share is stored as `33.33`
- [ ] Given the same event, when shares are summed, then the sum equals `100.00` exactly
- [ ] Given a payment, when it clears, then `payments.csv` contains the row
